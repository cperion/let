// §9.2 A mutable member written in place, then a partial move out of another member.
let run = do : Int
    let point mut = { let x mut = 1 let y mut = 2 };
    point.x = 5;
    let saved = move point.y;
    return point.x + saved
end
let answer = run()
