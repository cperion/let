# The HOST owns the world (§3.6): a host word is the only way to print, and a `Text` reaches it through the
# dictionary conversion `ToCString` -- the core has no view type of its own.
extern say (s : CString) : Int

let main = do : Unit
    say(ToCString with "hello, world")
end
